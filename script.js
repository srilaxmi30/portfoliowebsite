// Smooth scroll and form placeholder
const form = document.getElementById('contactForm');
form.addEventListener('submit', function(e){
    e.preventDefault();
    alert('Form submission is currently disabled. Add backend to process messages.');
});
